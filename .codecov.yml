codecov:
  notify:
    require_ci_to_pass: yes

coverage:
  status:
    project:
      default:
        # basic
        target: 0
        threshold: 0
    patch:
      default:
        target: 0
        threshold: 0

ignore:
  - 'ddmra/tests/'
  - 'ddmra/due.py'
  - 'ddmra/_version.py'
  - 'ddmra/info.py'
